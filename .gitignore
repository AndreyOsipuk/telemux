# Сборка
/dist/
/telemux
*.test

# Локальное окружение
.env
.env.*
!.env.example

# Фронт (SPA): node_modules не коммитим; dist КОММИТИМ (вшивается в бинарь через embed)
internal/server/web/node_modules/

# IDE
.idea/
.vscode/
.DS_Store

# Claude Code / AI-тулинг (не тащим в публичный репо)
.claude/
.claude.json
CLAUDE.md
.mcp.json
AGENTS.md
